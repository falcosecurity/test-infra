branch-protection:
  orgs:
    falcosecurity:
      repos:
        office-hours:
          enforce_admins: true # rules apply to admins
          protect: true # protect all branches
          restrictions:
            teams: ["admins"] # always allow team "admins" to push
          required_pull_request_reviews:
            dismiss_stale_reviews: true # automatically dismiss old reviews
            dismissal_restrictions: # allow review dismissals
              teams:
              - admins
            require_code_owner_reviews: true # require a code owner approval
            required_approving_review_count: 1 # number of approvals
          required_status_checks:
            strict: true # require PR branch to be up to date
        test-infra:
          enforce_admins: true
          protect: true
          restrictions:
            teams: ["admins"]
          required_pull_request_reviews:
            dismiss_stale_reviews: true # automatically dismiss old reviews
            dismissal_restrictions: # allow review dismissals
              teams:
              - admins
            require_code_owner_reviews: true # require a code owner approval
            required_approving_review_count: 1 # number of approvals
          required_status_checks:
            strict: true # require PR branch to be up to date

log_level: debug

pod_namespace: test-pods

prowjob_namespace: default

tide:
  merge_method:
    falcosecurity/office-hours: rebase
    falcosecurity/test-infra: rebase
  queries:
  - repos:
    - falcosecurity/office-hours
    labels:
    - approved
    - lgtm
    - "dco-signoff: yes"
    missingLabels:
    - do-not-merge
    - do-not-merge/hold
    - do-not-merge/invalid-owners-file
    - do-not-merge/work-in-progess
    - needs-rebase
  - repos:
    - falcosecurity/test-infra
    labels:
    - approved
    - lgtm
    - "dco-signoff: yes"
    missingLabels:
    - do-not-merge
    - do-not-merge/hold
    - do-not-merge/invalid-owners-file
    - do-not-merge/work-in-progess
    - needs-rebase