apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prow
configMapGenerator:
  - name: job-config
    files: []
    options:
      labels:
        app.kubernetes.io/part-of: prow
