# OCI Bootstrap

Administrative prerequisites for the [OCI platform](../), managed manually
and separately from the platform stack.

See [terraform.tfvars.example](terraform.tfvars.example) for the inputs.
